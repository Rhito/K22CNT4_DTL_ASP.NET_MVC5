@model IEnumerable<DtlLesson06CF.Models.DtlCategory>

@{
    ViewBag.Title = "DtlIndex";
}

<h2>DtlIndex</h2>

<p>
    @Html.ActionLink("Create New", "DtlCreate")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DtlCategoryName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DtlCategoryName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DtlId }) |
            @Html.ActionLink("Details", "Details", new { id=item.DtlId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DtlId })
        </td>
    </tr>
}

</table>
